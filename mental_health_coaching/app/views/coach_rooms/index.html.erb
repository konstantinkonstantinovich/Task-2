<% content_for :content do %>

<div id="room-id" data-room-id="<%= @room.try(:id) %>"></div>
<div class="row">
  <div class="col-4">
    <div class="row-width">
      <div class="row">
        <div class="col-12 mt">
          Users
        </div>
      </div>
      <br>
      <% @rooms.each do |room| %>
        <%= link_to room, class: 'without-link' do %>
          <% active_class = (@room == room) ? 'active-chat' : '' %>
            <div class="row <%= active_class %> chat-block">
              <div class="col-md-auto img-container">
                <% if room.user.avatar_user.attached? %>
                    <img src="<%= url_for(room.user.avatar_user) %>" class= "rounded-circle room-block-image">
                <% else %>
                  <img src="<%= asset_path('generic-user-icon-10.png') %>" class="rounded-circle room-block-image">
                <% end %>
              </div>
              <div class="col-sm-7">
                  <p class="room-block-name"><%= room.user.name %></p>
                  <p class="room-block-text"><%= room.messages.last.body if room.messages.last != nil %></p>
              </div>
              <div class="col-md-auto time-room">
                <%= since_created(room.messages.last.created_at) if room.messages.last != nil %>
              </div>
            </div>
        <% end %>
      <% end %>
    </div>


  </div>
  <div class="col-8">
    <div class="row-width">
      <div class="row">
        <div class="col-12 mt">
          Messages
        </div>
      </div>
    </div>
    <br>
    <% if @room.present? %>
      <div class="card min-height">
        <div class="card-body">
          <div class="message-content">
            <div class="row">
              <div class="col-12 date-now">
                <% @date = Date.today  %>
                — <%= @date.strftime("%B").upcase%> <%= Time.zone.now.day %> —
              </div>
              <br>
            </div>
                <div id="messages_coach" class="row layer-message">
                  <% @room.messages.order(:created_at).each do |message| %>
                      <%= render 'messages/message_coach', message: message  %>
                  <% end %>
                </div >
                <div id="messages_user" class="row layer-message disp"></div>
            <br>
            <div class="row-width">
                <%= render 'messages/form', message: Message.new, room: @room.id, user: nil, coach: Current.coach %>
            </div>

          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>


<br>

<script>
  $("#messages_coach").scrollTop($("#messages_coach")[0].scrollHeight);
</script>


<% end %>
<%= render template: "coach/show" %>
